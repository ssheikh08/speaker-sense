{
  "name": "text-transcription",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm build/lambda.zip && zip -r build/lambda.zip .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1430.0",
    "dotenv": "^16.3.1",
    "fluent-ffmpeg": "^2.1.2",
    "microsoft-cognitiveservices-speech-sdk": "^1.31.0"
  }
}
